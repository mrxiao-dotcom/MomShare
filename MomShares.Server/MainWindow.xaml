<Window x:Class="MomShares.Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MomShares.Server"
        xmlns:vm="clr-namespace:MomShares.Server.ViewModels"
        mc:Ignorable="d"
        Title="份额管理系统 - 服务器控制端" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Icon="app.ico">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 服务控制区域 -->
        <GroupBox Grid.Row="0" Header="服务控制" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Content="启动服务" 
                        Command="{Binding StartServiceCommand}"
                        Width="100" Height="30" Margin="0,0,10,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsServiceRunning}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="停止服务" 
                        Command="{Binding StopServiceCommand}"
                        Width="100" Height="30" Margin="0,0,10,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsServiceRunning}" Value="True">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <TextBlock Text="状态：" VerticalAlignment="Center" Margin="20,0,5,0"/>
                <TextBlock Text="{Binding ServiceStatus}" 
                           FontWeight="Bold"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Red"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsServiceRunning}" Value="True">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <!-- 配置区域 -->
        <GroupBox Grid.Row="1" Header="配置" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="API端口：" VerticalAlignment="Center" Margin="0,0,5,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ApiPort}" Margin="0,0,20,5"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="备份路径：" VerticalAlignment="Center" Margin="0,0,5,5"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding BackupPath}" Margin="0,0,0,5"/>

                <Button Grid.Row="1" Grid.Column="3" 
                        Content="保存配置" 
                        Command="{Binding SaveSettingsCommand}"
                        HorizontalAlignment="Right"
                        Width="100" Height="30"/>
            </Grid>
        </GroupBox>

        <!-- 日志区域 -->
        <GroupBox Grid.Row="2" Header="日志" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox Text="{Binding LogText}" 
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         FontFamily="Consolas"
                         FontSize="10"
                         Background="#F5F5F5"
                         BorderThickness="0"/>
            </ScrollViewer>
        </GroupBox>

        <!-- 底部信息 -->
        <TextBlock Grid.Row="3" 
                   Text="MomShares 产品份额管理系统 - 服务器控制端"
                   HorizontalAlignment="Center"
                   Foreground="Gray"
                   FontSize="10"/>
    </Grid>
</Window>
