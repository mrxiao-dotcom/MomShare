# 期货工具箱使用说明

## 概述

期货工具箱是一个独立的功能模块，与MomShares主系统相互独立，不需要登录即可使用。目前包含风险金计算器功能。

## 访问方式

### 直接访问
在浏览器中访问：`http://服务器IP:5000/futures`

例如：
- 本地访问：`http://localhost:5000/futures`
- 服务器访问：`http://192.168.1.100:5000/futures`

### 测试页面
打开项目根目录下的 `测试期货工具箱.html` 文件，可以快速测试功能。

## 功能说明

### 风险金计算器

#### 输入参数
- **账户初始权益**：账户开仓时的初始资金
- **当前权益**：账户当前的资金总额
- **初始净值**：默认为1.000（可修改）
- **当前净值**：自动计算 = 当前权益 ÷ 初始权益
- **防御净值**：止损线净值
- **目标净值**：止盈线净值
- **预警净值**：预警线净值（必须大于防御净值）
- **风险金笔数**：将要开的仓位数量

#### 图表说明
- **横轴**：风险金笔数（1到N）
- **纵轴**：净值
- **红色粗线**：目标净值
- **蓝色粗线**：当前净值
- **黄色线**：预警净值
- **黑色粗线**：防御净值
- **灰色虚线**：初始净值（1.0）
- **绿色方块**：建议风险金位置（当前净值到预警净值区间）
- **浅红色方块**：防御风险金位置（预警净值到防御净值区间）

#### 计算公式
```
建议单笔 = (当前权益 - 初始权益 × 预警净值) ÷ 风险金笔数
防御单笔 = (min(当前权益, 预警净值 × 初始权益) - 防御净值 × 初始权益) ÷ 风险金笔数
```

## 技术实现

### 架构特点
- **独立性**：不依赖主系统的认证和数据库
- **静态资源**：纯前端实现，无需后端计算
- **响应式设计**：支持移动端访问

### 文件结构
```
MomShares.Api/
├── Controllers/
│   └── FuturesController.cs      # API控制器（返回HTML页面）
├── wwwroot/
│   ├── futures.html             # 工具箱主页面
│   ├── futures.js               # 前端逻辑和图表绘制
│   └── styles.css               # 样式（扩展了现有样式）
└── WebAppBuilder.cs             # 路由配置（添加了/futures路径）
```

### 安全考虑
- 不需要身份验证即可访问
- 不涉及敏感数据操作
- 纯客户端计算，不存储用户数据

## 部署说明

### Windows服务器部署
1. 运行WPF应用程序 `MomShares.Server.exe`
2. 确保端口5000开放（运行 `配置防火墙.cmd`）
3. 在浏览器中访问 `http://服务器IP:5000/futures`

### Linux服务器部署
1. 发布API项目：`dotnet publish -c Release`
2. 运行：`./MomShares.Api`
3. 配置防火墙开放5000端口
4. 访问：`http://服务器IP:5000/futures`

## 故障排除

### 无法访问页面
1. 检查服务器是否正在运行
2. 检查端口5000是否被占用
3. 检查防火墙设置
4. 查看服务器日志

### 图表显示异常
1. 确保输入数据有效
2. 检查浏览器是否支持Canvas
3. 尝试刷新页面

### 计算结果不正确
1. 验证输入参数的合理性
2. 确认预警净值大于防御净值
3. 检查初始权益和当前权益的数值

## 扩展计划

### 功能二：待定
预留了"功能待定"的按钮和区域，方便后续添加新的期货分析工具。

### 可能的扩展功能
- 期货合约信息查询
- 持仓分析工具
- 风险评估报告
- 历史数据分析

## 版本信息

- **版本**：1.0.0
- **发布日期**：2025-01-01
- **兼容性**：支持所有现代浏览器


